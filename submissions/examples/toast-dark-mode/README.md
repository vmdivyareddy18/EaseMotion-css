# Toast Component - Dark Mode Support

This component demonstrates how to use CSS variables to implement a toggleable dark mode for a toast notification.

## Usage
- Click the "Toggle Dark Mode" button to switch themes.
- The CSS uses `:root` variables to define light/dark values.

## Why EaseMotion
By using CSS custom properties (variables), we ensure the component remains lightweight and avoids heavy JavaScript for simple theme switching.