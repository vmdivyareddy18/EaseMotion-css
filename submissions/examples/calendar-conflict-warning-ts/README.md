# Calendar Event Conflict Warning

**What does this do?** Marks an overlapping calendar event and explains the exact conflict interval.

**How is it used?** Position event buttons in the schedule and apply `conflict` to an overlapping event, linking it to a descriptive note.

**Why is it useful?** It combines pattern, border, text, and motion so scheduling conflicts remain clear without relying on color alone.
