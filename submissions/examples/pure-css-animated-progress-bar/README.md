# Pure CSS Animated Progress Bar

A dynamic, striped progress bar illustrating complex loading states entirely with CSS.

## Features
- Animated `background-position` applied to a repeating linear gradient creates the moving stripes.
- Dual keyframe animations attached to the same element (`width` + `background-position`).
- Highly accessible and performant.
