# Token Showcase Gallery

## What does this do?
Adds `.ease-token-gallery` — a visual gallery of design tokens (colors, spacing, type).

```html
<div class="ease-token-gallery">
  <div class="ease-token-row" style="--token-color: #6c63ff"></div>
</div>
```

Open `demo.html` to see it in action.