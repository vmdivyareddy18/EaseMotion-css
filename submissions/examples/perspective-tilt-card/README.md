# CSS Perspective Tilt Card

A purely CSS 3D perspective card that tilts upon hover.

## Features
- CSS `perspective` container
- `transform-style: preserve-3d` for nested 3D elements
- Uses `translateZ` to give depth to the text elements inside the card
