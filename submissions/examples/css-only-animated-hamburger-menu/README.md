# CSS-only Animated Hamburger Menu

A sleek, interactive navigation icon that morphs into a close button.

## Mechanics
- Uses hidden checkboxes to store state.
- Transforms multiple spans with `transform-origin` to create an 'X'.
- `cubic-bezier` timing function provides a satisfying snap.
