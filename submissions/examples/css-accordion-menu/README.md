# CSS Accordion Menu without JS

A fully functional accordion menu built using semantic HTML5 `<details>` and `<summary>` tags.

## Features
- No JavaScript required for toggling functionality
- Custom CSS arrow using `::after` pseudo-element
- Smooth dropdown animation using `@keyframes`
