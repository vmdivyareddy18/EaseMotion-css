# Cursor Pointer Demo Cards

Adds a pointer cursor to animation demo cards to indicate interactivity.

## Problem
Animation cards are clickable but do not visually communicate that they can be interacted with.

## Solution
Applied:

```css
cursor: pointer;
