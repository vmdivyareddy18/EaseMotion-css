# Scroll Padding Utilities

**What does this do?**  
Demonstrates `scroll-padding` with classes like `scroll-p-1`, `scroll-p-2`, `scroll-pt-2`, `scroll-pb-1`.

**Why?**  
Controls padding inside a scroll container for better visual spacing of scrolled content.
