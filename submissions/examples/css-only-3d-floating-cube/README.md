# CSS-Only 3D Floating Cube

This is the implementation for `CSS-Only 3D Floating Cube`.
Closes #17750.

## Usage
Include the CSS and use the `.ease-3d-floating-cube` class to see the animation effect.
