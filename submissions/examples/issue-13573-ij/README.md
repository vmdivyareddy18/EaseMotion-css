# ease-image-zoom

An image that scales up on hover within a fixed-size `overflow: hidden` container. Smooth CSS transition with no layout shift.

## Usage

Include `style.css` and wrap your `img` in a `.zoom-container` with fixed dimensions.

## Demo

Open `demo.html` in a browser.
