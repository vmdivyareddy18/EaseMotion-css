# CSS Splash Screen
This submission implements a CSS-only splash screen component.

- Uses `@keyframes` for a controlled fade-out transition.
- Includes a pulse animation to engage users during loading.
- Does not require JavaScript to manage the display state (auto-dismisses via `animation-fill-mode`).