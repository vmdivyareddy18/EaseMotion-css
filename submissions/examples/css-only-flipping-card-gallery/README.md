# CSS-only Flipping Card Gallery

A robust 3D card element that flips smoothly over the Y-axis when hovered.

## Mechanics
- Dependent on `perspective` on the parent container.
- Uses `backface-visibility: hidden` to properly render the back plane.
- Smooth bouncy finish using `cubic-bezier`.
