# ease-parallax-scroll

Mouse-driven parallax effect that moves nested layers at different speeds based on cursor position.

## Usage

Add `data-speed` attributes to `.parallax-layer` elements inside a container with `id="parallaxContainer"`. The script translates layers proportionally to cursor position on mousemove.
