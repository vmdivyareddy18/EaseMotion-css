# ease-cookie-banner

An animated cookie consent banner that slides up from the bottom with a smooth dismiss animation. Features styled consent buttons.

## Usage

Include `style.css` and add the `.cookie-banner` element with accept/decline buttons and the provided JS for dismiss.

## Demo

Open `demo.html` in a browser.
