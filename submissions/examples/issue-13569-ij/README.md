# ease-skeleton-image

Skeleton loading placeholder for images with shimmer animation.

## Usage

Include `style.css` and add the `ease-skeleton-image` class to a container. The placeholder uses aspect-ratio 16/9, a shimmer sweep animation, and an optional icon overlay.

## Demo

Open `demo.html` in a browser.
