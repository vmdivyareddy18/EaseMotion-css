# text-wrap: pretty vs balance Demo

This example demonstrates the difference between the CSS `text-wrap: balance` and `text-wrap: pretty` values.

## What is `text-wrap`?

The `text-wrap` property controls how text is wrapped across lines.

### Balance

```css
text-wrap: balance;
```