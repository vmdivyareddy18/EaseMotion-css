# Filter Chip Slide

A filter chip row with an active state that slides into place.

## Files

- `demo.html` - filter chip button group markup.
- `style.css` - active chip slide, hover/focus states, and reduced-motion support.

## Highlights

- Keeps chip dimensions stable during animation.
- Uses clear active styling for the selected filter.
- Preserves keyboard focus visibility.
