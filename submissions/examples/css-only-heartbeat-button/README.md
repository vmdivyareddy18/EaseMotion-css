# CSS-Only Heartbeat Button

This is the implementation for `CSS-Only Heartbeat Button`.
Closes #17816.

## Usage
Include the CSS and use the `.ease-heartbeat-button` class to see the animation effect.
