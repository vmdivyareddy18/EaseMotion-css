# Minified CSS Build

This submission fixes Issue #38163 by showing how to generate a minified production build (`ease-motion.min.css`).  
Minification removes whitespace and comments, reducing file size for faster load times.

## Usage

Include the minified file in your HTML:

```html
<link rel="stylesheet" href="ease-motion.min.css" />
