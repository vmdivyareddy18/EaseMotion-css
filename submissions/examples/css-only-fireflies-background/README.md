# CSS-Only Fireflies Background

This is the implementation for `CSS-Only Fireflies Background`.
Closes #17756.

## Usage
Include the CSS and use the `.ease-fireflies-background` class to see the animation effect.
