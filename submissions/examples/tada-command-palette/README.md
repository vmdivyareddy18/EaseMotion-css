# Minimalist Command Palette
A clean, streamlined command palette inspired by minimalist design patterns.

### Usage
Add the `.ease-command-palette` classes to your HTML structure as shown in `demo.html`.

### Why it's useful
It reduces visual clutter, allowing users to focus entirely on the search input, improving the overall UX of the interface.