# Gradient Button

## What does this do?
Gradient Button component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"gdb-primary\">Gradient Button Primary</div>
```

## Why is it useful?
Reusable gradient button UI element. Dark theme with CSS custom properties.
