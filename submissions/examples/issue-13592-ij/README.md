# Nav Underline Indicator

A primary navigation bar with a sliding active indicator that tracks hover and active states.

## Usage

Include `style.css` in your project and structure your nav with `class="nav"`, `class="nav-link"`, and a `#indicator` element.

## Demo

Open `demo.html` in a browser to see the component in action.
