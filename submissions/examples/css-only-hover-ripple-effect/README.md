# CSS-Only Hover Ripple Effect

This is the implementation for `CSS-Only Hover Ripple Effect`.
Closes #17828.

## Usage
Include the CSS and use the `.ease-hover-ripple-effect` class to see the animation effect.
