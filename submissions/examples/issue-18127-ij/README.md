# ease-infinite-scroll

A CSS-only looping horizontal scroll using `translateX` animation with duplicated content for seamless looping.

**Usage:** Include `style.css` only. The `.scroll-content` element animates `translateX` from 0 to -50% over 20 seconds. The content must be duplicated (second copy) for seamless loop. Adjust `animation-duration` for speed.
