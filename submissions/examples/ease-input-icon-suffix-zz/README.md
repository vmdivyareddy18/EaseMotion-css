# Icon Suffix Input

## What does this do?
Icon Suffix Input component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"isi-primary\">Icon Suffix Input Primary</div>
```

## Why is it useful?
Reusable icon suffix input UI element. Dark theme with CSS custom properties.
