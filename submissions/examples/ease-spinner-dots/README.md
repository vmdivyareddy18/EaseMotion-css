# Spinner Dots

A CSS-only bouncing dot loading indicator with three dots that bounce up sequentially. Each dot uses staggered animation delays via custom properties, creating a cascading wave effect suitable for loading states, buffering indicators, and async operations.
