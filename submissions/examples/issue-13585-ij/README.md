# Logo Cloud

An infinite horizontal scrolling logo showcase (marquee style) for "trusted by" sections, with pause-on-hover and masked fade edges.

## Usage

Include `style.css` in your project. Use `.logo-cloud` with `.logo-track` containing `.logo-item` elements.

## Demo

Open `demo.html` in a browser to see the component in action.
