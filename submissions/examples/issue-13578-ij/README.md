# ease-carousel-fade

A crossfade carousel/slideshow that transitions between slides using opacity. CSS-only via radio buttons with auto-play option using animation-delay sequencing.

## Usage

Include `style.css` and structure your carousel with `.carousel-fade` container and `.slide` items controlled by radio inputs.

## Demo

Open `demo.html` in a browser.
