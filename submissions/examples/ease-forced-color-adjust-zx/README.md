# Forced Color Adjust Utilities

**What does this do?**  
Demonstrates `forced-color-adjust` with classes `forced-auto` and `forced-none`.

**Why?**  
Controls whether the browser can apply forced colors (Windows High Contrast mode). Use `forced-none` to preserve custom styling in accessibility color modes.
