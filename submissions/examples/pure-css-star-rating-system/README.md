# Pure CSS Star Rating System

A robust, interactive 5-star rating widget without a single line of JavaScript.

## Features
- Relies on hidden radio inputs and `label` elements.
- Uses `row-reverse` flexbox alongside the `~` combinator for intuitive hover logic.
- Includes scale and color transitions for high-quality interactions.
