# Pulse Notification Center

A responsive notification center demo with animated pulse indicators, hover lift cards, and keyboard focus states.

## Files

- `demo.html` contains the notification panel markup.
- `style.css` contains the responsive layout, pulse animation, hover lift, and focus-visible styling.

## Usage

Open `demo.html` in a browser. Hover the notifications or tab into them to view the motion and focus states.
