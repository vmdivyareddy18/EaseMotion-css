# Icon Prefix Input

## What does this do?
Icon Prefix Input component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"ipi-primary\">Icon Prefix Input Primary</div>
```

## Why is it useful?
Reusable icon prefix input UI element. Dark theme with CSS custom properties.
