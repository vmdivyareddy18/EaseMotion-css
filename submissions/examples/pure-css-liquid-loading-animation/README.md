# Pure CSS Liquid Loading Animation

A soothing liquid fill animation constrained to a circular container.

## Architecture
- Uses an oversized square with a `border-radius: 40%` rotating infinitely.
- Combines `rotate` and `top` animations to simulate splashing liquid rising.
- Text incorporates `mix-blend-mode` for a modern aesthetic.
