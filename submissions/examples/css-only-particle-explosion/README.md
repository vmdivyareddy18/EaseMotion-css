# CSS-Only Particle Explosion

This is the implementation for `CSS-Only Particle Explosion`.
Closes #17768.

## Usage
Include the CSS and use the `.ease-particle-explosion` class to see the animation effect.
