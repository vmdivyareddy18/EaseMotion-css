# Animation-Delay Utilities
This submission introduces utilities to control the start time of CSS animations.

- `.delay-0` to `.delay-1000`: Standardized delay increments for sequencing animations.

These are essential for creating staggered, staggered-fade, or sequential reveal effects in UI components.