# ease-back-to-top

A floating animated scroll-to-top button that fades in/out based on scroll position. Uses minimal JS for class toggling and smooth scroll behavior.

## Usage

Include `style.css` and add the `.back-to-top` button with the provided JS scroll handler.

## Demo

Open `demo.html` in a browser.
