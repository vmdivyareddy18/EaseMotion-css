# ease-glitch-text

A CSS-only glitch effect using `::before` and `::after` pseudo-elements with offset transforms and color channels.

**Usage:** Include `style.css` only. Add the `.glitch` class to any text element and set `data-text` attribute matching the text content. Adjust `clip-path` insets and color values for different glitch styles.
