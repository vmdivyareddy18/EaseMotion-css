# ease-loading-bar-top

Top-of-page loading bar (YouTube-style). A thin fixed-position progress bar that animates during page load and fades out on completion.

## Usage

Include `style.css` and add `.ease-loading-bar-top > .bar` to your page. Toggle `.active` to start and `.complete` to finish.

## Demo

Open `demo.html` in a browser.
