# Size Selector Lift

1. What does this do? Adds a lift and ring animation to selectable product size chips.
2. How is it used? Apply `.size-selector` to the group and `.size-chip` to each size option.
3. Why is it useful? It makes choice controls feel responsive and easier to scan in product interfaces.
