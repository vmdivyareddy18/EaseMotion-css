# CSS-Only Vinyl Record Spin

This is the implementation for `CSS-Only Vinyl Record Spin`.
Closes #17765.

## Usage
Include the CSS and use the `.ease-vinyl-record-spin` class to see the animation effect.
