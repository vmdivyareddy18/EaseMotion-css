# Background-Attachment Utilities
This submission introduces utilities to control how a background image is fixed within its container during scrolling.

- `.bg-fixed`: Image remains stationary relative to the viewport.
- `.bg-local`: Image scrolls with the element's content.
- `.bg-scroll`: Image scrolls with the container itself (default).

These utilities enable parallax-like effects and maintain branding or patterns during user interactions.