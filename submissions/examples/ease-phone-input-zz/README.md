# Phone Input

## What does this do?
Phone Input component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"ph-primary\">Phone Input Primary</div>
```

## Why is it useful?
Reusable phone input UI element without JavaScript. Dark theme with CSS custom properties.
