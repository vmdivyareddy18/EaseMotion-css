# Password Input

## What does this do?
Password Input component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"pw-primary\">Password Input Primary</div>
```

## Why is it useful?
Reusable password input UI element without JavaScript. Dark theme with CSS custom properties.
