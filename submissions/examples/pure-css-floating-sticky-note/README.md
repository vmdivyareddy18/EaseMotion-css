# Pure CSS Floating Sticky Note

A skeuomorphic post-it note design with CSS generated pins and corners.

## Features
- Uses radial gradients for a realistic red push-pin.
- Slightly rotated with a shadow to simulate being stuck to a board.
- Lifting hover animation creates a tactile feel.
