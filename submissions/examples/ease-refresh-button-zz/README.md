# Refresh Button

## What does this do?
Refresh Button component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"rf-primary\">Refresh Button Primary</div>
```

## Why is it useful?
Reusable refresh button UI element without JavaScript. Dark theme with CSS custom properties.
