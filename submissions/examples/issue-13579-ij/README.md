# ease-timeline-vertical

An animated vertical timeline component with scroll-triggered entry animations, connecting lines, and dot markers. Supports alternating left/right layout.

## Usage

Include `style.css` and structure your timeline with `.timeline` container, `.timeline-item` entries, and `.timeline-dot` markers.

## Demo

Open `demo.html` in a browser.
