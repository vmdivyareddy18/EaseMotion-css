# Spinning Border Beam Effect

A CSS animated gradient border that continuously spins around a card edge using conic gradients and mask compositing.

## Files

- `demo.html` - Card with spinning gradient border beam
- `style.css` - Conic gradient rotation, mask compositing

## Usage

Open `demo.html` to see the border beam continuously spin around the card.

Closes #19244
