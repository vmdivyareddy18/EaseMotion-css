# Table Row Hover Highlight

1. What does this do? Adds a sliding highlight and subtle lift to a table row on hover or focus.
2. How is it used? Apply `.highlight-table` to a table and `.highlight-row` to rows that need motion feedback.
3. Why is it useful? It improves scanability for dense data tables while keeping the behavior CSS-only.
