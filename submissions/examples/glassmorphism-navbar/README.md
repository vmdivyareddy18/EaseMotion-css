# Glassmorphism Sticky Navigation Bar

A modern sticky navigation bar using the glassmorphism design trend.

## Features
- CSS `position: sticky` for scroll behavior
- CSS `backdrop-filter: blur()` for frosted glass effect
- Semi-transparent background with soft borders
