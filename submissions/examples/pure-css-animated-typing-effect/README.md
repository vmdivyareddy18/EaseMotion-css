# Pure CSS Animated Typing Effect

A classic terminal-style typing animation using pure CSS steps.

## Highlights
- Relies on the `ch` unit and `steps()` timing function.
- Creates a blinking cursor effect via `border-right` animation.
- Zero JavaScript overhead.
