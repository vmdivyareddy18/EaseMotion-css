# CSS Animated Underline Menu

A clean navigation menu with a slick animated underline effect on hover.

## Features
- CSS `::after` pseudo-element for the underline
- `transform: scaleX()` and `transform-origin` for the sliding animation direction
- Smooth color transitions
