# Announce Bar Reduced Motion Fix

### Overview
Adds a `@media (prefers-reduced-motion: reduce)` query to the `.ease-announce-bar` to disable animations for users sensitive to motion.

### Fits EaseMotion
Ensures accessibility by default without breaking the experience for standard users.