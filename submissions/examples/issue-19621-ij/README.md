---
title: Battery Charge
issue: 19621
type: animation
technique: width keyframes + box-shadow glow
---

A battery icon whose fill level pulses between empty and full. The green `linear-gradient` fill animates via `width` keyframes while a dynamic `box-shadow` glow intensifies at peak charge to suggest active charging.
