# CSS Hexagon Grid Layout

A responsive grid layout composed of pure CSS hexagons.

## Features
- Uses CSS `clip-path` for the hexagon shape
- Flexbox grid with negative margins for interlocking tiles
- Hover effects with smooth scaling
