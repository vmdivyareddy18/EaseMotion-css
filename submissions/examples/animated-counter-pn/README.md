# Animated Scroll Counter Component

A lightweight scroll-triggered utility component that animates numeric elements up to a target value using an Intersection Observer and modern CSS custom properties.