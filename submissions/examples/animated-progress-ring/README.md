# Animated Progress Ring

A sleek, pure CSS animated progress ring component that leverages `conic-gradient` and CSS variables for customizability.

## Features
- Fully responsive
- Uses custom CSS properties (`--percent`, `--color`) for easy state management
- Clean internal structure avoiding SVG bloat

## Usage
Add the `progress-ring` class and define the `--percent` variable inline.
