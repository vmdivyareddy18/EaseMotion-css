# CSS Morphing Blob Background

An organic, fluidly animating blob background shape driven by CSS.

## Technique
- Employs an 8-value `border-radius` to define irregular shapes.
- Continuously shifts radii values inside `@keyframes` for a morphing liquid effect.
- Can be used as a dynamic framing element or ambient background.
