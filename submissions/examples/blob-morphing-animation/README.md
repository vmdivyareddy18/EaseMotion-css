# CSS Blob Morphing Animation

An organic blob shape that smoothly morphs and rotates using pure CSS `border-radius` transitions.

## Features
- Complex multi-value `border-radius` morphing
- Fluid `ease-in-out` infinite animation
- Gradient background and glow effect
