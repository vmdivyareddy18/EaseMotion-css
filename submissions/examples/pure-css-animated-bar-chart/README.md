# Pure CSS Animated Bar Chart

A dynamic visualization chart configured entirely through CSS.

## Execution
- Relies on CSS Custom Properties (`var(--target-height)`) declared in inline styles for modularity.
- `animation: forwards` locks the bar height into place.
- Sequential delays wait for the bars to finish rising before revealing labels.
