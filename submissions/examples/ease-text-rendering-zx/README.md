# Text Rendering Utilities

**What does this do?**  
Demonstrates `text-rendering` with classes `render-speed`, `render-legibility`, `render-precision`.

**Why?**  
Controls text rendering optimization for speed vs. legibility vs. geometric precision.
