# CSS-Only Morphing Blob Shape

This is the implementation for `CSS-Only Morphing Blob Shape`.
Closes #17815.

## Usage
Include the CSS and use the `.ease-morphing-blob-shape` class to see the animation effect.
