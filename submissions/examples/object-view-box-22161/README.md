# Object-View-Box Utilities
This submission introduces utilities to control the `object-view-box` CSS property.

- `.view-box-full` to `.view-box-center-strip`: Defines a sub-region of an image or video to be displayed.

This property allows for non-destructive cropping and viewport manipulation of replaced elements.