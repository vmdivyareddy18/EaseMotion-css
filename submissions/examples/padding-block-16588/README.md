# Padding-Block Utilities
This submission provides logical `padding-block` utilities for controlling spacing in the block axis.

- `.pb-0` to `.pb-8`: Standardized spacing increments.

Logical properties ensure consistent spacing regardless of the document's writing mode (vertical or horizontal).