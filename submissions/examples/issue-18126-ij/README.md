# ease-split-text-reveal

Splits text into word `<span>` elements and staggers their reveal with a spring-like upward motion.

**Usage:** Include `style.css` and the inline script from `demo.html`. Target any text element with `id="splitText"`. Each word animates with a 120ms staggered delay. Adjust `transitionDelay` multiplier for different pacing.
