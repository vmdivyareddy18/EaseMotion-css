# Pure CSS Typewriter Text Animation

A simple but effective typewriter text animation using CSS `steps()` and `border-right`.

## Features
- CSS `steps()` timing function for character-by-character appearance
- Blinking cursor using border animations
- Zero JavaScript
