# Sync Button

## What does this do?
Sync Button component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"sy-primary\">Sync Button Primary</div>
```

## Why is it useful?
Reusable sync button UI element without JavaScript. Dark theme with CSS custom properties.
