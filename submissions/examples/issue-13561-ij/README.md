# ease-avatar-group

Overlapping avatar stack with hover spread. Negative margin creates overlap; on group hover, avatars shift apart slightly. Includes +N overflow indicator.

## Usage

Include `style.css` and use `.ease-avatar-group` with `.avatar` children. Use inline styles or classes for gradient backgrounds.

## Demo

Open `demo.html` in a browser.
