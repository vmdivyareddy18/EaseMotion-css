# Build Stack — Blocks Stacking Up

CSS blocks that stack upward with staggered entrance. Each block drops in with a bounce, building a tower sequentially.

## Files

- `demo.html` — eight block elements
- `style.css` — staggered drop-in keyframes, block gradient

## Usage

Open `demo.html` to watch the blocks stack into a tower.

Closes #21281
