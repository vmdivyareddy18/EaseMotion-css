# Slider Input

## What does this do?
Slider Input component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"si-primary\">Slider Input Primary</div>
```

## Why is it useful?
Reusable slider input UI element without JavaScript. Dark theme with CSS custom properties.
