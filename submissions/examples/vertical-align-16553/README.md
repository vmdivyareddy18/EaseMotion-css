# Vertical-Align Utilities
This submission introduces utilities to control the vertical alignment of inline, inline-block, and table-cell elements.

- `.align-baseline`, `.align-top`, `.align-middle`, `.align-bottom`, `.align-text-top`, `.align-text-bottom`.

These utilities are vital for adjusting the vertical positioning of icons, images, and text snippets within a single line.