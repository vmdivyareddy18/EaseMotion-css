# CSS-Only Water Drop Ripple

This is the implementation for `CSS-Only Water Drop Ripple`.
Closes #17766.

## Usage
Include the CSS and use the `.ease-water-drop-ripple` class to see the animation effect.
