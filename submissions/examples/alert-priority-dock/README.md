# Alert Priority Dock

A responsive operations alert dock with priority rails, hover lift cards, and keyboard focus states.

## Files

- `demo.html` contains the alert dock markup.
- `style.css` contains the priority color system, card lift interaction, and responsive layout.

## Usage

Open `demo.html` in a browser. Hover an alert or tab into it to see the rail expand and the card lift.
