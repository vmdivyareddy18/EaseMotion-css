# Pure CSS Smooth Accordion

An incredibly clean and smooth CSS accordion component avoiding standard `max-height` workarounds.

## Innovation
- Uses the new modern `display: grid; grid-template-rows: 0fr -> 1fr;` transition technique.
- Avoids jittery animations common in older CSS accordions.
- Lightweight and accessible design.
