# ease-magnetic-hover

Magnetic hover effect that pulls an element toward the cursor position within its bounding box.

## Usage

Add `id="magneticBtn"` to any element. Adjust `strength` factor in the script to control pull distance. Element returns to origin on mouseleave.
