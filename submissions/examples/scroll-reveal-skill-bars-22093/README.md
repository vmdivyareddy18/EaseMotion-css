# Animated Skill Bars with Scroll Reveal
This submission implements skill bars that animate upon entering the viewport.

- Uses CSS `@keyframes` for smooth expansion.
- Leverages the modern `animation-timeline: view()` property to trigger the animation based on scroll position.
- Uses CSS variables (`--skill-level`) for easy customization of skill percentages.