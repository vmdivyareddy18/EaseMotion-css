# Responsive Aspect-Ratio Container Wrapper (`.ease-aspect-container`)

A fluid, structural wrapper utility designed to preserve scaling geometry for complex animations across fluid viewport environments.

## Implementation Standard

```html
<div class="ease-aspect-container" style="--ease-aspect-ratio: 16/9;">
  <div class="your-animation-content">
    </div>
</div>
```