# Newsletter Form

An animated newsletter signup form with multi-state submit button (idle → loading spinner → success checkmark) and glowing input focus.

## Usage

Include `style.css` in your project and structure your form with the provided classes.

## Demo

Open `demo.html` in a browser to see the component in action.
