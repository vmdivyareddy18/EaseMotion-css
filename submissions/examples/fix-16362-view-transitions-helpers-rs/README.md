# Fix: View Transitions Helpers

## Description
This adds CSS helper classes for the View Transitions API, standardizing fade animations.

## How it works
- Assigns a unique `view-transition-name`.
- Overrides default `::view-transition-old` and `::view-transition-new` pseudo-elements with custom keyframes.
