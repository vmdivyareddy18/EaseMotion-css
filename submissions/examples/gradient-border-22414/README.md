# Gradient Border Animation
Adds a utility class for an animated gradient border effect.

- **Effect**: Uses a wrapper element with a linear-gradient background that rotates via `hue-rotate`.
- **Use Case**: Highlighting interactive cards, call-to-action buttons, or creating eye-catching UI borders without complex SVG masking.