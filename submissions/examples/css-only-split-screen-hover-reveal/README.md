# CSS-Only Split Screen Hover Reveal

This is the implementation for `CSS-Only Split Screen Hover Reveal`.
Closes #17822.

## Usage
Include the CSS and use the `.ease-split-screen-hover-reveal` class to see the animation effect.
