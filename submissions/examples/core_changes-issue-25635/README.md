1. What does this do? Types text character by character with a blinking cursor, with support for replay and reduced motion.
2. How is it used? Add `class="typewriter"` to an element, call `startTypewriter()` to begin typing.
3. Why is it useful? A typewriter effect adds personality to hero sections, terminal interfaces, and storytelling layouts — this implementation is lightweight, accessible, and configurable.
