# Magnetic Nav Link
An interactive navigation component that "attracts" the mouse cursor.

- Uses `getBoundingClientRect` to calculate the distance between the mouse and link center.
- `cubic-bezier` easing provides a natural, springy feel when moving the mouse over the link.
- Highly effective for creative portfolios and high-end landing pages.