# Contain Utilities

CSS utility classes for the `contain` property.

## Usage

```html
<div class="contain-none">...</div>
```

## Classes

- `.contain-none` — contain: none;
- `.contain-strict` — contain: strict;
- `.contain-content` — contain: content;
- `.contain-size` — contain: size;
- `.contain-layout` — contain: layout;
- `.contain-paint` — contain: paint;
- `.contain-style` — contain: style;
- `.contain-inline-size` — contain: inline-size;
- `.contain-multi` — contain: layout paint;
- `.contain-multi-2` — contain: layout paint size;
- `.contain-multi-3` — contain: layout style paint;
- `.contain-multi-4` — contain: strict layout paint size;
- `.contain-inherit` — contain: inherit;
- `.contain-initial` — contain: initial;
- `.contain-unset` — contain: unset;
- `.contain-revert` — contain: revert;
- `.contain-revert-layer` — contain: revert-layer;
- `.contain-default` — contain: none;
- `.size-contain` — contain: size;
- `.layout-contain` — contain: layout;

## Responsive

- `sm:` prefix for 640px+
- `lg:` prefix for 1024px+

## Dark Mode

- `dark:` prefix for `prefers-color-scheme: dark`
## Reduced Motion

- `motion-safe:` prefix for `prefers-reduced-motion: reduce`
