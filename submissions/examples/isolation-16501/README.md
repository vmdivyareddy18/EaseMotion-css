# Isolation Utilities
This submission introduces utilities to control the `isolation` CSS property.

- `.isolate`: Creates a new stacking context to limit the scope of `mix-blend-mode`.
- `.isolate-auto`: Resets the isolation to the default behavior.

These utilities are essential for creating controlled compositing effects in complex UI layouts without unwanted blending artifacts.