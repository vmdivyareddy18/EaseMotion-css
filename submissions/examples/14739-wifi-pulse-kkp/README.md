# Wifi Signal Pulse

1. What does this do? Animates layered wifi arcs with staggered opacity pulses.
2. How is it used? Apply `.wifi-pulse` to the wrapper and add `.wifi-arc` spans for each signal band.
3. Why is it useful? It communicates connectivity, pairing, or network scanning states with a small CSS-only effect.
