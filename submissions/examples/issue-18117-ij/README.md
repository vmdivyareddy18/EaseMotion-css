# ease-particles-bg

Animated particle network background using Canvas. Particles drift slowly and connect via lines when within a threshold distance.

## Usage

Include a `<canvas id="particlesCanvas">` inside `.container`. The script auto-sizes to the parent and spawns ~60 particles with connection lines.
