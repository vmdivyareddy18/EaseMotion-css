# Disabled Input

## What does this do?
Disabled Input component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"dsi-primary\">Disabled Input Primary</div>
```

## Why is it useful?
Reusable disabled input UI element. Dark theme with CSS custom properties.
