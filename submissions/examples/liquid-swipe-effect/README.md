# Liquid Swipe Effect

A CSS-only implementation of a liquid swipe transition between two pages.

## Features
- Uses CSS `clip-path` for expanding circular mask
- Smooth cubic-bezier transitions
- Zero JavaScript needed
