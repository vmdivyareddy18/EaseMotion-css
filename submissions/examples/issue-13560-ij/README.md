# ease-chip-removable

Animated removable filter chip. Shows × button on hover; on remove, width collapses to 0 with opacity fade (overflow: hidden for clean collapse animation).

## Usage

Include `style.css` and use `.ease-chip-removable` with child `.remove-btn`. Toggle `.removing` class to trigger collapse animation.

## Demo

Open `demo.html` in a browser.
