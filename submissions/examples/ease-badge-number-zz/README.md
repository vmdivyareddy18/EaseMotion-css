# Number Badge

## What does this do?
Number Badge component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"nbd-primary\">Number Badge Primary</div>
```

## Why is it useful?
Reusable number badge UI element. Dark theme with CSS custom properties.
