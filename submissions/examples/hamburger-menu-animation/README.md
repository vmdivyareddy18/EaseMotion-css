# CSS Hamburger Menu Animation

A hamburger menu icon that fluidly morphs into a close (X) icon using CSS transitions.

## Features
- Pure CSS state management via hidden checkbox input
- Smooth cubic-bezier transitions on `transform` and `opacity`
- Responsive lines with `transform-origin` adjustments
