# CSS-Only Paper Plane Flight

This is the implementation for `CSS-Only Paper Plane Flight`.
Closes #17762.

## Usage
Include the CSS and use the `.ease-paper-plane-flight` class to see the animation effect.
