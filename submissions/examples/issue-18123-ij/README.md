# ease-confetti-burst

Generates a colorful particle burst of confetti pieces with random colors, sizes, and trajectories.

**Usage:** Include `style.css` and the inline script from `demo.html`. Click the button to spawn 80 confetti pieces. Each piece follows a parabolic trajectory with rotation. Adjust `count`, `vel`, and `colors` in the script.
