# Overscroll Behavior Utilities
Utility classes to control browser overscroll behavior.

- **overscroll-none**: Completely disables the browser's default scroll chaining and bounce effects.
- **overscroll-contain**: Prevents scroll propagation to ancestor elements while allowing native local effects.
- Ideal for modal windows, sidebars, and nested scrollable areas.