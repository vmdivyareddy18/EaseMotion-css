# Rotating Gradient Background

A CSS background that continuously rotates the gradient angle using the `@property` rule and registered custom property animation.

## Files

- `demo.html` - Rotating gradient with speed controls
- `style.css` - @property registration, angle animation

## Usage

Open `demo.html` to see the gradient rotate. Use the buttons to change speed.

Closes #19231
