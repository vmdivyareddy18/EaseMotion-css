# Loading Button

## What does this do?
Loading Button component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"lb2-primary\">Loading Button Primary</div>
```

## Why is it useful?
Reusable loading button UI element. Dark theme with CSS custom properties.
