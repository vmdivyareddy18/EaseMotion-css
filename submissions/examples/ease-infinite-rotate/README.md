# Infinite Rotate Loader

A CSS-only infinite rotating ring loader using `@keyframes` spin animation. The loader uses a bordered circle with one colored segment to create the rotating effect, suitable as a loading indicator for any web application.
