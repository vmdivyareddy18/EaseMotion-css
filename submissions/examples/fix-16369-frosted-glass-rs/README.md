# Fix: Backdrop Filter / Frosted Glass Utility Classes

## Description
Adds utility classes to easily create "frosted glass" (glassmorphism) effects using `backdrop-filter`.

## How it works
- Provides `.ease-backdrop-blur-*` utilities for different blur radiuses.
- Provides a base `.ease-bg-glass` for the semi-transparent overlay.
