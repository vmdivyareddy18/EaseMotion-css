# Backdrop Fade Modal

A modal dialog with animated backdrop and entry transition.

## Usage

Open `demo.html` and click "Open Modal".

## Example

Uses the `<dialog>` element with `::backdrop` fade animation and `@starting-style`.
