# ease-counter-badge-pop

Notification counter badge that pops/bounces when the number changes. Scale animation on value change, green flash on increase, works with JS updates.

## Usage

Include `style.css` and add `.ease-counter-badge-pop` to a badge element. Trigger `.pop` class via JS to animate.

## Demo

Open `demo.html` in a browser.
