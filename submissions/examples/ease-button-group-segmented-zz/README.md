# Segmented Group

## What does this do?
Segmented Group component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"sg2-primary\">Segmented Group Primary</div>
```

## Why is it useful?
Reusable segmented group UI element. Dark theme with CSS custom properties.
