# ease-blob-morph

A CSS-only continuously morphing blob shape using `border-radius` keyframe animation.

**Usage:** Include `style.css` only. The `.blob` element cycles through 5 border-radius configurations over 6 seconds. Modify `@keyframes blob-morph` to create different organic shapes.
