# Animated Progress Ring Tracker

1. What does this do? Displays a circular progress indicator that animates to its value.
2. How is it used? Wrap an SVG with `.progress-ring-sap` and use `.ring-fill-sap` to define the animated stroke.
3. Why is it useful? Offers a visually appealing way to display statistics or loading progress.