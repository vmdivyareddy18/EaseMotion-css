# CSS Typing Effect
This submission provides a typewriter animation effect using pure CSS.

- Uses `@keyframes` to animate the `width` of the text container.
- Uses `steps()` timing function to create the distinct character-by-character typing motion.
- Includes a blinking cursor effect via `border-right` color toggling.