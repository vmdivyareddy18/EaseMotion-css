# Range Input

## What does this do?
Range Input component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"ri-primary\">Range Input Primary</div>
```

## Why is it useful?
Reusable range input UI element without JavaScript. Dark theme with CSS custom properties.
