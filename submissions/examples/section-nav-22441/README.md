# Page Section Navigation
A vertical navigation component for long-form content.

- Uses `position: sticky` to keep navigation visible while scrolling.
- Simple, border-left indicators for active section status.
- Lightweight, semantic HTML structure using `nav` and `section` tags.