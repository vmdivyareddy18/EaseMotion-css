# Cursor Crosshair Utility

CSS-only demo for a `cursor-crosshair` utility class. The example uses targeting panels that expose the crosshair cursor on hover.

## Files

- `demo.html` - targeting panel markup
- `style.css` - cursor utility, target styling, and responsive layout

## Usage

Open `demo.html` and hover the target panels.

Closes #10801
