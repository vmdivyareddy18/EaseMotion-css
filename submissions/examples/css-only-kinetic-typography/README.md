# CSS-Only Kinetic Typography

This is the implementation for `CSS-Only Kinetic Typography`.
Closes #17751.

## Usage
Include the CSS and use the `.ease-kinetic-typography` class to see the animation effect.
