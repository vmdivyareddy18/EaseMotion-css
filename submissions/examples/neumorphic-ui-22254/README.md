# Neumorphic UI Component Set
This component set implements the "Soft UI" design trend using CSS box-shadows.

- Uses double-shadows to simulate depth and light-sourcing.
- `box-shadow` properties are inverted on `:active` to create a "pressed" effect.
- Easily customizable via CSS variables for different base background colors.