# Cloud Sync Animation

1. What does this do? Animates a cloud icon with rotating sync arrows and a soft upload pulse.
2. How is it used? Apply `.cloud-sync` to the wrapper and include `.sync-arrow` elements around the cloud shape.
3. Why is it useful? It communicates syncing, backup, or remote-save status with a compact dependency-free CSS effect.
