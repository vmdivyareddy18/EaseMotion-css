# Footer Reveal

A footer that uses sticky positioning to reveal itself with a parallax-like effect as the user scrolls past the main content.

## Usage

Include `style.css` in your project. Structure uses `.page-wrap` with `.main-content` above `.footer-reveal`.

## Demo

Open `demo.html` in a browser to see the component in action.
