# GPS Locating Animation

1. What does this do? Shows a location pin with expanding radar rings to suggest active GPS locating.
2. How is it used? Apply `.gps-locator` to the wrapper and include `.gps-ring` spans around `.gps-pin`.
3. Why is it useful? It gives maps, delivery flows, and location pickers a focused CSS-only searching state.
