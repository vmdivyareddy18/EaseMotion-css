# Small Input

## What does this do?
Small Input component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"smi-primary\">Small Input Primary</div>
```

## Why is it useful?
Reusable small input UI element. Dark theme with CSS custom properties.
