# ease-orbital-loader

A CSS-only orbital loader with concentric rings rotating at different speeds and directions.

**Usage:** Include `style.css` only. The `.orbit` container holds three `.orbit-ring` elements with `.orbit-dot` satellites and a center dot. Adjust `animation-duration` per ring for different speeds.
