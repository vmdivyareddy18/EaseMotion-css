# ease-skeleton-text

Skeleton loading lines for text content with shimmer animation.

## Usage

Include `style.css` and use `.ease-skeleton-text` containing `.line` divs. The last line is shorter (60% width) for a natural paragraph look. Configurable line count.

## Demo

Open `demo.html` in a browser.
