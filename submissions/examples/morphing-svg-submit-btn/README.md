# Morphing SVG Submit Button

An interactive button that morphs into a circular loading/success state using pure CSS transitions and SVG stroke animations.

## Features
- CSS-driven width morphing from a pill to a circle
- SVG path animations for the checkmark using `stroke-dasharray`
- Fully responsive and standalone
