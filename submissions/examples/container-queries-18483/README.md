# Container Queries
Implementation of modern container queries for component-level responsiveness.

- **container-type**: Sets the containment context (e.g., `inline-size`).
- **@container**: Allows styling based on the size of the parent element rather than the viewport.
- Enables truly modular UI components that adapt to their placement within a layout.