# ease-context-menu

An animated right-click style context menu with stagger-in list items.

## Usage

Include the `style.css` in your project and use the provided HTML structure with `.context-menu` and `.menu-item` classes. The demo uses JavaScript for right-click triggering and positioning.

## Demo

Open `demo.html` in a browser to see the component in action.
