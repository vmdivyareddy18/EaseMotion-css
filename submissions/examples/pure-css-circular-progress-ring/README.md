# Pure CSS Circular Progress Ring

A modern dashboard indicator using `conic-gradient` to create a filling circle.

## Details
- Animates a `conic-gradient` using keyframes.
- Achieves a hollow ring by overlaying an `inner-circle`.
- Clean and highly performant alternative to SVG stroke-dasharray tricks.
