# Ease Scroll Scale

An animation effect that ties element scaling directly to the scroll position using the CSS `scroll()` timeline.

## Usage
Add `scroll-scale` class to any element:
```html
<div class="scroll-scale">Content</div>