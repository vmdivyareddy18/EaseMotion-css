# Morphing Shape Utility
Adds a utility class for fluid, organic shape animations.

- **Effect**: Uses `border-radius` with complex values and `keyframes` to create a liquid-like morphing effect.
- **Use Case**: Creating eye-catching background elements, decorative UI blobs, or subtle interactive feedback indicators.