# CSS-only Isometric Grid Layout

A 3D isometric representation of a standard CSS grid.

## Configuration
- Skews the entire grid using `rotateX` and `rotateZ`.
- Employs layered `box-shadow` to simulate 3D block thickness.
- Interactively pops out via `translateZ` on hover.
