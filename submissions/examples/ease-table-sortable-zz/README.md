# Sortable Table

## What does this do?
Sortable Table component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"st2-primary\">Sortable Table Primary</div>
```

## Why is it useful?
Reusable sortable table UI element. Dark theme with CSS custom properties.
