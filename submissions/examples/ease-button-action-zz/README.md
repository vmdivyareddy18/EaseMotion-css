# Action Button

## What does this do?
Action Button component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"acb-primary\">Action Button Primary</div>
```

## Why is it useful?
Reusable action button UI element. Dark theme with CSS custom properties.
