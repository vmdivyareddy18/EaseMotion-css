# Number Ticker Counter
A vertical scrolling ticker for numerical data.

- Uses `@keyframes` to animate the `translateY` property.
- Fixed `height` and `overflow: hidden` create the "window" effect.
- Pure CSS implementation provides performant animations for statistics or counters.