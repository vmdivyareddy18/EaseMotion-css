# Color-Interpolation Utilities
This submission introduces utilities to control the color interpolation space.

- `.interpolate-srgb`: Standard sRGB color space.
- `.interpolate-linear`: Linear RGB space for perceptually accurate gradients.

These properties are primarily used in SVG and complex visual effects to ensure transitions between colors behave as expected mathematically.