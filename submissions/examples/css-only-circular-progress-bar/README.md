# CSS-Only Circular Progress Bar

This is the implementation for `CSS-Only Circular Progress Bar`.
Closes #17826.

## Usage
Include the CSS and use the `.ease-circular-progress-bar` class to see the animation effect.
