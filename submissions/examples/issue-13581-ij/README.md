# Testimonial Card

An animated testimonial/review card with decorative quote marks, hover lift effect, star ratings, and avatar with name/role.

## Usage

Include `style.css` in your project and use the `.testimonial-card` class.

## Demo

Open `demo.html` in a browser to see the component in action.
