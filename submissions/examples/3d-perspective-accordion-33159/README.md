# 3D Perspective Tilt Accordion

An interactive, responsive UI accordion menu built completely on native CSS transform properties.

## Engineering Performance
- **3D Matrix Transformations**: Leverages `perspective` and `preserve-3d` layout mechanics to shift plane angles dynamically on card hover vectors.
- **Fluid Structural Design**: Entire implementation runs completely inside its designated sandbox folder without risking global document layout leaks.