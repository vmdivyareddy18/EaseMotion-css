# Interactive 3D Flipping Card

A pure CSS 3D flipping card that rotates on hover to reveal content on the back.

## Features
- Uses CSS `perspective` and `transform-style: preserve-3d`
- Smooth cubic-bezier rotation transitions
- Hidden backface for realistic 3D flipping effect
