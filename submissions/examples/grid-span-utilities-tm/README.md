# grid-column & grid-row span utilities

Grid layout utilities for controlling how elements span across grid columns and rows.

## Properties Covered

`grid-column`, `grid-row`, `grid-column-start`, `grid-column-end`, `grid-row-start`, `grid-row-end`

## Usage

Import the stylesheet and apply utility classes to your elements:

```html
<link rel="stylesheet" href="easemotion.css" />
<div class="...">Content</div>
```

## Classes

| Class | Effect |
|-------|--------|
| See `style.css` | Full list of generated utilities |

## Responsive

All utilities include responsive variants with `sm:` and `md:` prefixes for mobile and tablet breakpoints.

## Dark Mode

Dark mode variants are automatically applied via `@media (prefers-color-scheme: dark)`.

## Accessibility

Respects `prefers-reduced-motion` — all transitions are disabled for users who prefer reduced motion.

---

*Generated by EaseMotion-css automation — grid-span-utilities-tm*
