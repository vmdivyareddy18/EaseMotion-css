# CSS Scroll Progress Indicator
This component utilizes modern Scroll-driven Animations to display a progress bar.

- Uses `animation-timeline: scroll(root)` to map scroll position to animation progress.
- Requires no JavaScript for calculating scroll percentages.
- Provides a performant, native solution for reading-progress indicators.