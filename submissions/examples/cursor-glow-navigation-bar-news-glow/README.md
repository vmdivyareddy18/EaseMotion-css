# Cursor Glow Navigation Bar

A responsive navigation component containing micro-glow tracking effects optimized for portal templates.

## Details
- Tracks pointer events locally using relative custom variable coordinates.
- Provides semantic layout navigation entirely styled within pure layout configurations.