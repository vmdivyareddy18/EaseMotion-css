# Isolation v2 Utilities
This submission refines the isolation utility implementation.

- `.isolate-new`: Ensures a clean stacking context using `isolation: isolate`.
- `.isolate-reset`: Reverts to browser default `isolation: auto`.

These utilities are critical for developers managing complex blend modes and z-index layers in modern UI designs.