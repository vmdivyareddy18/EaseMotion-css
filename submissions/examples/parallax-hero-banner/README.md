# Parallax Scrolling Hero Banner

A pure CSS parallax scrolling effect utilizing 3D transforms.

## Features
- Hardware-accelerated CSS `perspective` and `translateZ` for multi-layered parallax
- Scaled back layers to prevent clipping
- Zero JavaScript for scrolling sync
