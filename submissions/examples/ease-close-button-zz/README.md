# Close Button

## What does this do?
Close Button component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"cl-primary\">Close Button Primary</div>
```

## Why is it useful?
Reusable close button UI element without JavaScript. Dark theme with CSS custom properties.
