# CSS Animated Skeleton Loader

A modern skeleton loading screen card featuring a shimmering gradient animation.

## Features
- Pure CSS `linear-gradient` used for the shimmer effect
- Continuous `background-position` animation
- Realistic card layout simulating an image, title, and paragraph
