# CSS-Only Shimmering Text Loading Effect

This is the implementation for `CSS-Only Shimmering Text Loading Effect`.
Closes #17824.

## Usage
Include the CSS and use the `.ease-shimmering-text-loading-effect` class to see the animation effect.
