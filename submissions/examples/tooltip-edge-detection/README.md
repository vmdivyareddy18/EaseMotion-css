# Tooltip Edge Detection

An example demonstrating how to implement edge-aware tooltips using data attributes (`data-align="start"` and `data-align="end"`) to prevent viewport overflow without requiring JavaScript.

## Features
- Pure CSS tooltips
- Edge-aware alignment modifiers
- Smooth opacity and transform transitions
