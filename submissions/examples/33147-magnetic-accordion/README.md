# Magnetic Pull Accordion

A pure CSS accordion component that uses `cubic-bezier` for a "magnetic" pull-in effect when focused.

## How to use
Add the `accordion-item` class to your container. The magnetic pull is triggered on focus/click.