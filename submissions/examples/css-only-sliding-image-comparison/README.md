# CSS-only Sliding Image Comparison

A before-and-after image slider that leverages native CSS features.

## Functionality
- Uses the native `resize: horizontal` property for interactive dragging.
- The foreground image container expands/shrinks via user resizing.
- Extremely lightweight and performant.
