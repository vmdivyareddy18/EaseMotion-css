# ease-cursor-follower

Custom cursor with a fast-following dot and a smooth lagging ring.

## Usage

Include `.cursor-dot` and `.cursor-ring` fixed elements. The dot tracks the cursor instantly; the ring follows with lerp interpolation for a trailing effect.
