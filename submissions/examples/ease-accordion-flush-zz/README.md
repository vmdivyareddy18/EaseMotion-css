# Accordion Flush

## What does this do?
Accordion Flush component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="acf-primary">Accordion Flush Primary</div>
```

## Why is it useful?
Reusable accordion flush UI element. Dark theme with CSS custom properties.
