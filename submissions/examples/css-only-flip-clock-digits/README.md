# CSS-Only Flip Clock Digits

This is the implementation for `CSS-Only Flip Clock Digits`.
Closes #17755.

## Usage
Include the CSS and use the `.ease-flip-clock-digits` class to see the animation effect.
