# Morph Card Hover
A playful component that morphs its shape and appearance on interaction.

- Uses `cubic-bezier` for a "bouncy" transition effect.
- Transitions `border-radius` from 20px to 50% to achieve the morphing shape.
- Pure CSS implementation for high performance.