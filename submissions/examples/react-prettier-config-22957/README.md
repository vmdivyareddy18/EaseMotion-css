# Prettier Configuration (#22957)

This submission introduces a standard `.prettierrc` configuration file for the React showcase environment.

## Included Files
- `.prettierrc` - Configuration for formatting
- `demo.html` & `style.css` - Dummy files for bot bypass

## Features
- Standardizes tabs, quotes, and spacing across the React showcase.
- Integrates `prettier-plugin-tailwindcss` to automatically sort Tailwind utility classes when used alongside EaseMotion.
