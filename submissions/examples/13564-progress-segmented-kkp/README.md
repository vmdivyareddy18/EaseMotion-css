# Segmented Progress Bar

CSS-only segmented step progress bar demo. The example animates completed segments and highlights the current step.

## Files

- `demo.html` - step progress markup
- `style.css` - segment layout, fill animation, and responsive state styling

## Usage

Open `demo.html` in a browser to view the segmented progress bar.

Closes #13564
