# Text-align-last Utility
Adds utilities for the `text-align-last` CSS property.

- **Purpose**: Controls the alignment of the last line of a block of text or a line immediately before a forced line break.
- **Use Case**: Improving the readability and aesthetic balance of justified text blocks by preventing orphans or awkward alignments on the final line.