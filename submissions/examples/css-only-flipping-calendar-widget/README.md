# CSS-only Flipping Calendar Widget

A mechanical flipping calendar animation using pure CSS.

## Features
- Implements `transform-origin` and `rotateX` to simulate physical flipping.
- Segmented numbers using overflow cropping.
- Seamless infinite keyframe loop.
