# Progress Stepper Bar
This component provides a visual indicator for multi-step workflows.

- Uses a pseudo-element (`::before`) to draw the connecting line between steps.
- Simple class-based toggling (`active`, `completed`) for state management.
- Fully responsive and easily customizable via CSS variables.