# User-Modify Utilities
This submission introduces utilities to control the editability of elements using the CSS `user-modify` property.

- `.user-modify-read-only`: Prevents user modification of the element content.
- `.user-modify-read-write`: Allows the user to edit the content of the element.

These utilities are useful for creating content-editable UI components without standard input fields.