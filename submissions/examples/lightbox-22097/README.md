# CSS Lightbox with Target Selector
This submission creates a functional lightbox image gallery using only CSS.

- Uses the `:target` pseudo-class to show the lightbox when a link points to the image's ID.
- Requires zero JavaScript to open or close the gallery items.
- Provides a lightweight, accessible alternative to library-heavy solutions.