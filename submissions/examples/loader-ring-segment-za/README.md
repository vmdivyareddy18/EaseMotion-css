# Segmented Ring Loader Animation

A CSS segmented ring spinner with four colored segments rotating with staggered delays. Perfect for loading states.

## Files

- `demo.html` - Ring loader with pulsing text
- `style.css` - Segmented border animation with staggered delays

## Usage

Open `demo.html` to see the segmented ring loader spin.

Closes #19254
