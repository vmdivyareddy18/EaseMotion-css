# CSS-Only Checkmark Draw Animation

This is the implementation for `CSS-Only Checkmark Draw Animation`.
Closes #17818.

## Usage
Include the CSS and use the `.ease-checkmark-draw-animation` class to see the animation effect.
