# Resource Quota Panel

A responsive dashboard demo with animated quota meters, hover lift cards, and keyboard focus states.

## Files

- `demo.html` contains the resource quota panel markup.
- `style.css` contains the quota meter animation, card interactions, and responsive layout.

## Usage

Open `demo.html` in a browser. Hover or focus a quota card to see the lift state while the usage meter animates.
