# Orphans Utilities
This submission introduces utilities to control the `orphans` property, which defines the minimum number of lines that must remain at the top of a page or column when a break occurs.

- `.orphans-1` to `.orphans-3`: Configures the minimum line count for the start of the block.

When combined with `widows`, this provides complete control over typographic flow and pagination quality.