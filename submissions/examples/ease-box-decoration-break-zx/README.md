# Box Decoration Break Utilities

**What does this do?**
Demonstrates `box-decoration-break` with `slice` and `clone` classes.

**Why?**
Controls how background, padding, border, and other decorations render across inline element line breaks.
