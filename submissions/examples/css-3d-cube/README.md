# CSS 3D Cube Animation

A continuously rotating 3D cube built entirely with CSS 3D transforms.

## Features
- CSS `preserve-3d` and `perspective`
- Semi-transparent, distinctively colored faces
- Smooth infinite rotation animation
