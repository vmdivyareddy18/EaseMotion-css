# Hover Table

## What does this do?
Hover Table component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"ht2-primary\">Hover Table Primary</div>
```

## Why is it useful?
Reusable hover table UI element. Dark theme with CSS custom properties.
