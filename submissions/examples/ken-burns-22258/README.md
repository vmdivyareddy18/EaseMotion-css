# Ken Burns Effect
This component creates the classic "Ken Burns" panning and zooming effect on images.

- Uses `@keyframes` to animate `scale` and `translate` properties simultaneously.
- `infinite alternate` ensures a smooth, continuous motion.
- `object-fit: cover` ensures the image fills the container regardless of aspect ratio.