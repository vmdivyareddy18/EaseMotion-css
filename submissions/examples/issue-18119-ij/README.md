# ease-shimmer-waves

Shimmer wave effect using a rotating `conic-gradient` on a pseudo-element that spans beyond the container.

## Usage

Add `<div class="shimmer-wave">` inside `.container`. The gradient rotates continuously via `@keyframes shimmerRotate` for a wavy light-sweep effect.
