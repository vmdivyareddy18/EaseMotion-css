# ease-3d-tilt-card

A card that tilts in 3D perspective based on mouse position using CSS `rotateX` and `rotateY`.

**Usage:** Include `style.css` and the inline script from `demo.html`. The `.tilt-inner` element transforms relative to cursor position within the card. Adjust the rotation multiplier (default 30) for tilt intensity.
