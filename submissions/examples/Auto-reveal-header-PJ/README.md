# Auto Reveal Header

## Description
A scroll-aware navigation header that automatically hides when the user scrolls down and reappears when the user scrolls up.

## Features
- Hides on downward scroll
- Reappears on upward scroll
- Smooth transition animation
- Improves navigation accessibility on long pages

## Usage
Include the HTML structure and CSS styles, then use the JavaScript scroll listener to toggle the header visibility.