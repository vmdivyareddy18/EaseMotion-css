# CSS-Only Glitch Text Reveal

This is the implementation for `CSS-Only Glitch Text Reveal`.
Closes #17830.

## Usage
Include the CSS and use the `.ease-glitch-text-reveal` class to see the animation effect.
