# ease-ripple-button

A button that creates an expanding circular ripple effect on each click.

**Usage:** Include `style.css` and the inline script from `demo.html`. The ripple originates at the click position and scales up while fading out. Ripple color is semi-transparent white; adjust in `.ripple` background.
