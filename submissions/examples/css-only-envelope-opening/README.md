# CSS-Only Envelope Opening

This is the implementation for `CSS-Only Envelope Opening`.
Closes #17763.

## Usage
Include the CSS and use the `.ease-envelope-opening` class to see the animation effect.
