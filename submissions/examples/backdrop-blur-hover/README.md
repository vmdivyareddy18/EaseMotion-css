# Backdrop Blur Hover

A simple hover animation that smoothly increases the backdrop blur effect.

## Features

- Smooth hover transition
- Uses CSS variable `--ease-backdrop-blur-amount`
- Transparent glassmorphism card
- Uses `backdrop-filter: blur(8px)` on hover

## Files

- `demo.html`
- `style.css`
