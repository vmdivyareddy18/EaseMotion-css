# CSS-Only Frosted Glass Profile Card

This is the implementation for `CSS-Only Frosted Glass Profile Card`.
Closes #17823.

## Usage
Include the CSS and use the `.ease-frosted-glass-profile-card` class to see the animation effect.
