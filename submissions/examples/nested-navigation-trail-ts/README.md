# Nested Navigation Active Trail

**What does this do?** Connects an active nested page to its expanded parent sections with a visible navigation trail.

**How is it used?** Build nested `details` branches and apply `current` with `aria-current="page"` to the active link.

**Why is it useful?** It preserves orientation in deep navigation through hierarchy, line cues, focus styles, responsive layout, and optional motion.
