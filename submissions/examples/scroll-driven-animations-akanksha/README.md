# Native Scroll-Driven Animation Utilities

High-performance CSS layout utilities implementing the modern browser-native `animation-timeline` specifications.

## Utility Classes
* `.timeline-scroll` - Ties existing movement keyframe layers to parent structural scroll distance tracking.
* `.timeline-view` - References active layout entry transitions dynamically as items move inside the viewport.