# Comparison Table

An animated feature comparison/pricing table with staggered checkmark animations, row hover highlights, and responsive card layout on mobile.

## Usage

Include `style.css` in your project and structure your table with the provided classes.

## Demo

Open `demo.html` in a browser to see the component in action.
