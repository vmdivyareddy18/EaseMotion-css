# ease-lightbox

An animated image lightbox/modal viewer using CSS-only `:target` technique. Click a thumbnail to open a fullscreen view with scale+fade entrance, backdrop blur, and close on backdrop click.

## Usage

Include `style.css` and structure your gallery with `.gallery` thumbnails linked to `.lightbox` overlay targets.

## Demo

Open `demo.html` in a browser.
