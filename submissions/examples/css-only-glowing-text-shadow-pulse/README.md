# CSS-Only Glowing Text Shadow Pulse

This is the implementation for `CSS-Only Glowing Text Shadow Pulse`.
Closes #17829.

## Usage
Include the CSS and use the `.ease-glowing-text-shadow-pulse` class to see the animation effect.
