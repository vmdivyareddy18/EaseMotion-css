# Pulse Button Effect

A continuous subtle scale pulse animation for Call-to-Action buttons.

## Usage

```html
<button class="ease-pulse-button">Get Started</button>
```

## Why it fits EaseMotion CSS

Pure CSS, no JS, human-readable class name. Draws attention to CTA buttons without being distracting. Respects `prefers-reduced-motion`.