# Mega Menu

An animated multi-column dropdown menu that expands smoothly from navigation items on hover.

## Usage

Include `style.css` in your project. Use `.nav-item` with `.mega-menu` containing `.mega-column` divs.

## Demo

Open `demo.html` in a browser to see the component in action.
