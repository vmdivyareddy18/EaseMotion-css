# Export Button

## What does this do?
Export Button component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"ex-primary\">Export Button Primary</div>
```

## Why is it useful?
Reusable export button UI element without JavaScript. Dark theme with CSS custom properties.
