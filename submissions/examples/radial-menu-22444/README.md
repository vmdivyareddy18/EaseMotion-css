# Radial Context Menu
This component implements a circular context menu using CSS positioning.

- Uses `transform: rotate` and `translate` to distribute menu items evenly around a center point.
- Provides a compact, gesture-friendly navigation alternative.
- Easily extensible to include more items by adjusting the rotation degrees.