# Fix: Typing Cursor Customization CSS Variables

## Description
Adds CSS variables to customize the typing cursor effect easily.

## How it works
- Uses `--ease-cursor-color`, `--ease-cursor-width`, and `--ease-cursor-style`.
- Animates the right border to simulate a blinking text cursor.
