# Animation-Play-State Utilities
This submission introduces utility classes for toggling the play state of CSS animations.

- `.pause-animation`: Sets `animation-play-state` to `paused`.
- `.running-animation`: Sets `animation-play-state` to `running`.

These utilities are essential for creating interactive UI components that react to user events (e.g., pausing a carousel or a loading indicator).