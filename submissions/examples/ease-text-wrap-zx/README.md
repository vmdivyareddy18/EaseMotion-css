# Text Wrap Utilities

**What does this do?**  
Demonstrates `text-wrap` with classes `text-wrap`, `text-nowrap`, `text-balance`, `text-pretty`.

**Why?**  
Controls text wrapping behavior for better readability and layout.
