# Closeable Tag

## What does this do?
Closeable Tag component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"clt-primary\">Closeable Tag Primary</div>
```

## Why is it useful?
Reusable closeable tag UI element. Dark theme with CSS custom properties.
