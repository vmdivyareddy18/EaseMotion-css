# Countdown Digit

A CSS-only countdown animation where digits appear with a pop effect, scale up, and then fade out sequentially. Each digit is staggered using CSS custom properties, suitable for launch screens, game intros, or timer transitions.
