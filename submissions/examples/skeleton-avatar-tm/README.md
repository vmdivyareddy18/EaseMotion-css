# Skeleton Avatar

## What does this do?
Adds `.ease-skeleton-avatar` — a circular placeholder for an avatar with a shimmer effect.

```html
<div class="ease-skeleton-avatar ease-skeleton-avatar-lg"></div>
```

Open `demo.html` to see it in action.