# CSS Glitch Text Effect

A cyberpunk-inspired text glitch effect using pseudo-elements, `clip-path`, and multiple animations.

## Features
- RGB split using `text-shadow`
- Dynamic slice animations using `clip-path: polygon()`
- Completely pure CSS
