# ease-typewriter

Typewriter effect that types and deletes phrases in a loop with a blinking cursor.

## Usage

Add an element with `id="typewriterText"` and a sibling with `class="typewriter-cursor"`. The script cycles through `phrases` array with configurable typing / deleting speeds.
