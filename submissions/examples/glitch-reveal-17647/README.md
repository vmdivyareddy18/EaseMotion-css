# CSS-Only Glitch Reveal
This component implements a cinematic "glitch" reveal effect using standard CSS properties.

- Uses the legacy `clip` property with `@keyframes` to create rapid, fragmented slicing.
- Achieves a high-energy, digital aesthetic without JavaScript.
- Lightweight and performant for landing page hero sections.