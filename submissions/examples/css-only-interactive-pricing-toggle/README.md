# Interactive Pricing Toggle

A pricing component that dynamically swaps prices and timeframes based on a pure CSS toggle interaction.

## Overview
- Leverages the general sibling combinator `~` to manipulate child nodes across the DOM tree.
- Smooth transitions and fade-in animations on price switch.
- Robust, accessible hidden checkbox logic.
