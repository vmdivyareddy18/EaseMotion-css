# Interactive Timeline
A sequential timeline component featuring staggered reveal animations.

- Uses `nth-child` to sequence animation delays.
- Clean CSS-only approach using `keyframes` for entry transitions.
- Responsive design suitable for project histories or documentation flow.