# CSS Floating Astronaut Animation

A charming, zero-gravity floating animation loop.

## Design
- Multi-stage keyframes combining both `translateY` and `rotate`.
- SVG background pattern utilized directly within CSS for stars.
- Smooth `ease-in-out` timing to simulate weightlessness.
