# ease-stagger-fade

Staggers the fade-in of multiple elements using `setTimeout` with increasing delays.

**Usage:** Include `style.css` and the inline script from `demo.html`. Each `.stagger-item` fades in sequentially with a 150ms delay between items. Adjust the delay multiplier in the script for different pacing.
