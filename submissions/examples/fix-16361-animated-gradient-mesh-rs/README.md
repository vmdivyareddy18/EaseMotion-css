# Fix: Animated Gradient Mesh

## Description
This component provides a fluid, animated gradient mesh background using multiple radial gradients and background-position animation.

## How it works
- Uses `radial-gradient` backgrounds on top of a solid color.
- Animates `background-position` to create a smooth, flowing effect.
