# Scroll Snap Container
A CSS-based scroll snapping primitive.

- **Effect**: Uses `scroll-snap-type` and `scroll-snap-align` to enforce precise scroll positioning.
- **Use Case**: Creating image carousels, horizontal scroll decks, or full-page snap scrolling experiences without JavaScript.