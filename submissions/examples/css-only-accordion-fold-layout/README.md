# CSS-Only Accordion Fold Layout

This is the implementation for `CSS-Only Accordion Fold Layout`.
Closes #17770.

## Usage
Include the CSS and use the `.ease-accordion-fold-layout` class to see the animation effect.
