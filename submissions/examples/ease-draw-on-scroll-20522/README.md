# SVG Draw on Scroll (#20522)
An animation effect where an SVG path reveals itself progressively as the user scrolls.
## Features
- Hardware-accelerated SVG stroke animation.
- Customizable path length and speed.
- Lightweight, CSS-only approach.
