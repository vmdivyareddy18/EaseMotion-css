# Cursor Zoom In Utility

CSS-only demo for a `cursor-zoom-in` utility class. The example shows interactive preview cards that expose the zoom-in cursor on hover.

## Files

- `demo.html` - preview markup
- `style.css` - cursor utility and card styling

## Usage

Open `demo.html` and hover the preview cards.

Closes #10786
