# ease-gallery-masonry

A Pinterest-style animated masonry image gallery using CSS columns. Images fade/scale in with staggered delays and have a hover zoom effect.

## Usage

Include `style.css` and structure your gallery with `.masonry` container and `.masonry-item` entries for images.

## Demo

Open `demo.html` in a browser.
