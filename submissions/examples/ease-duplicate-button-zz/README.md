# Duplicate Button

## What does this do?
Duplicate Button component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"du-primary\">Duplicate Button Primary</div>
```

## Why is it useful?
Reusable duplicate button UI element without JavaScript. Dark theme with CSS custom properties.
