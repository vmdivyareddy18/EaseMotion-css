# ease-progress-segmented

Segmented step progress bar for multi-step forms. Each segment fills with a smooth animation, the active segment pulses, and completed segments show filled state.

## Usage

Include `style.css` and use `.ease-progress-segmented` with `.segment` children. Toggle `.filled` and `.active` classes via JS.

## Demo

Open `demo.html` in a browser.
