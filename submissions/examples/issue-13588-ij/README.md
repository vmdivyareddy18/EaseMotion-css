# Social Share

An animated social share button group where icons expand from a central share button with staggered reveal and brand-specific hover colors.

## Usage

Include `style.css` in your project and use the `.share-group` wrapper with `.share-icons` containing `.share-icon` buttons.

## Demo

Open `demo.html` in a browser to see the component in action.
