# Neumorphic Toggle Checkbox Switch

A soft-UI toggle mimicking a physical sliding switch.

## Properties
- Combines inset shadows for the track and outset shadows for the thumb.
- Uses `cubic-bezier` for a satisfying physical snap when checked.
- Fully accessible via the hidden checkbox hack.
