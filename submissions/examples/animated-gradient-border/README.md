# Animated Gradient Border

A card with a moving gradient border on hover.

## Usage

Open demo.html and hover over the card.

## Example

Uses a ::before pseudo-element with a gradient that shifts on hover.
