# ease-notification-center

A dropdown notification panel with an animated bell icon, staggered list items, and unread indicator dots.

## Usage

Include `style.css` and add the `.notification-center` component with bell trigger and dropdown panel.

## Demo

Open `demo.html` in a browser.
