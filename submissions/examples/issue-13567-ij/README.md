# ease-empty-state

Animated empty state illustration container. Features a floating/breathing icon, fade-in text, and a pulsing CTA button.

## Usage

Include `style.css` and use `.ease-empty-state` with an `.icon-box`, heading, paragraph, and `.cta-btn`.

## Demo

Open `demo.html` in a browser.
