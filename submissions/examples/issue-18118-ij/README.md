# ease-noise-texture

Animated noise texture overlay using an SVG `<feTurbulence>` filter embedded as a CSS background-image on a pseudo-element.

## Usage

Add `<div class="noise-overlay">` inside `.container`. The noise shifts subtly via `@keyframes noiseShift`. Uses `mix-blend-mode: overlay` for a film-grain look.
