# Directional Gap Utilities

Adds CSS-only directional gap utilities (`.ease-gap-x-*` and `.ease-gap-y-*`) to provide fine-grained control over horizontal and vertical spacing in flex and grid layouts.

## Usage

Control horizontal spacing only:
```html
<div class="ease-flex ease-gap-x-4">...</div>