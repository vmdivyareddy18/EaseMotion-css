# Liquid Gooey Button

A mesmerizing button hover effect that creates a "gooey" liquid separation feel.

## Technical Implementation
- Combines CSS transforms with SVG `<filter>` (`feColorMatrix`).
- Highly interactive and playful UI element.
- Accessible markup with clean separation of concerns.
