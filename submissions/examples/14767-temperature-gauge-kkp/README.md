# Temperature Gauge Fill

1. What does this do? Animates a thermometer-style gauge with rising fill and a soft pulse at the active temperature level.
2. How is it used? Apply `.temperature-gauge` to the wrapper and set the fill height with `.temperature-fill`.
3. Why is it useful? It turns status, sensor, or dashboard temperature feedback into a readable CSS-only motion pattern.
