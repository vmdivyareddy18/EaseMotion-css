# Error Input

## What does this do?
Error Input component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class=\"eri-primary\">Error Input Primary</div>
```

## Why is it useful?
Reusable error input UI element. Dark theme with CSS custom properties.
