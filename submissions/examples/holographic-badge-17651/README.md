# CSS-Only Holographic ID Badge
A visual component simulating a holographic security card.

- Uses `linear-gradient` and absolute positioning for the shimmer.
- Animation cycle creates the moving "shine" effect.
- Zero dependencies; purely CSS-driven design.