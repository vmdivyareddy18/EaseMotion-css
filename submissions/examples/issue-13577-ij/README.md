# ease-carousel-dots

A CSS-only carousel with animated navigation dots. Uses the radio input + label technique with smooth slide transitions. The active dot scales up and changes color.

## Usage

Include `style.css` and structure your carousel with `.carousel-dots` container, radio inputs, and dot navigation.

## Demo

Open `demo.html` in a browser.
