# Pure CSS 3D Flipping Pricing Table

An elegant space-saving 3D flip card for pricing toggles.

## Details
- Utilizes `perspective` and `transform-style: preserve-3d`.
- `backface-visibility: hidden` keeps the reverse side invisible until flipped.
- Excellent for toggling between Monthly and Annual pricing without JavaScript.
