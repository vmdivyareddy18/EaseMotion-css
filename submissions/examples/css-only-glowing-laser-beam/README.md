# CSS-Only Glowing Laser Beam

This is the implementation for `CSS-Only Glowing Laser Beam`.
Closes #17767.

## Usage
Include the CSS and use the `.ease-glowing-laser-beam` class to see the animation effect.
