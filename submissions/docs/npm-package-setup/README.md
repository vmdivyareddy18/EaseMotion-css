# NPM Package Setup

This submission fixes Issue #40440 by adding documentation for an npm package setup.  
Developers can now install EaseMotion CSS with `npm install ease-motion-css`.

## Usage

1. Install:
```bash
npm install ease-motion-css
